import PassoStack from "../components/PassoStack";
import TextoCentral from "../components/TextoCentral";

export interface IPropsEl {
  navigation?: object;
}

export default (props: any) => {
  return (
      <TextoCentral corFundo="#e53935">Tela A</TextoCentral>
  );
};
